import { ModulePlaceholder } from "@/components/dashboard/module-placeholder"

export default function ProgressPage() {
  return (
    <ModulePlaceholder
      title="Progress"
      description="Weight, steps, calories, and charts will connect to this view next — using the same local storage approach as workouts."
    />
  )
}
